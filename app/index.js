const {
  Animation,
  Character,
  Player
} = require('./Character.js');
const {
  Dialog,
  DialogParser
} = require('./Dialog.js');
const Place = require('./Place.js');

module.exports = {
  Animation,
  Character,
  Player,
  Dialog,
  DialogParser,
  Place
};